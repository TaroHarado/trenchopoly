# Чеклист для деплоя на Vercel

## ✅ Подготовка (выполнено)

- [x] Логотип перемещен в `public/logo.png`
- [x] Данные экспортированы: `prisma/seed-data.json` (16 users, 34 games)
- [x] Создан `.env.example` с примерами переменных
- [x] Создан `vercel.json` с конфигурацией
- [x] Обновлен `package.json` с командами для экспорта/импорта
- [x] Обновлен `.gitignore` (исключает seed-data.json)
- [x] Создан `DEPLOY.md` с инструкциями

## 📋 Шаги для деплоя

### 1. Подготовка GitHub репозитория

```bash
# Проверьте, что все файлы добавлены
git status

# Добавьте все файлы (кроме .env и dev.db)
git add .

# Создайте коммит
git commit -m "Prepare for Vercel deployment"

# Если репозиторий еще не создан на GitHub:
# 1. Создайте новый репозиторий на GitHub
# 2. Добавьте remote:
git remote add origin https://github.com/yourusername/trenchopoly.git
git branch -M main
git push -u origin main
```

### 2. Настройка Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите ваш репозиторий `trenchopoly`
5. Нажмите **"Import"**

### 3. Настройка переменных окружения в Vercel

В настройках проекта Vercel → **Settings** → **Environment Variables**, добавьте:

```
DATABASE_URL=postgresql://... (будет создано позже)
JWT_SECRET=<сгенерируйте случайную строку>
NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta
NEXT_PUBLIC_SOLANA_RPC_ENDPOINT=https://api.mainnet-beta.solana.com
HOUSE_WALLET_PUBLIC_KEY=EzNPUsVEBtm5tr5BeWk1V1sB2q8LnWbHZkPLfqNvFarN
SOCKET_PORT=3001
```

**Важно:** `DATABASE_URL` добавите после создания Postgres базы.

### 4. Создание Vercel Postgres базы данных

1. В панели Vercel перейдите в **Storage**
2. Нажмите **"Create Database"**
3. Выберите **"Postgres"**
4. Выберите план (Hobby - бесплатный)
5. Нажмите **"Create"**
6. Скопируйте **Connection String**
7. Добавьте его в переменные окружения как `DATABASE_URL`

### 5. Обновление Prisma для PostgreSQL

В `prisma/schema.prisma` измените:

```prisma
datasource db {
  provider = "postgresql"  // было: "sqlite"
  url      = env("DATABASE_URL")
}
```

### 6. Деплой и миграции

После первого деплоя:

1. Vercel автоматически запустит `npm run build`
2. После успешного деплоя, подключитесь к Vercel через CLI или используйте Vercel Dashboard

Для применения миграций:

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в Vercel
vercel login

# Подключитесь к проекту
vercel link

# Примените миграции
npx prisma migrate deploy
```

Или используйте Vercel Dashboard → **Deployments** → выберите последний деплой → **Settings** → **Environment Variables** → добавьте команду в **Build Command**:

```
prisma generate && prisma migrate deploy && next build
```

### 7. Импорт данных

После применения миграций, импортируйте сохраненные данные:

```bash
# Установите DATABASE_URL локально
export DATABASE_URL="your-vercel-postgres-connection-string"

# Импортируйте данные
npm run import-data
```

Или используйте Vercel CLI для выполнения команды:

```bash
vercel env pull .env.local
npm run import-data
```

### 8. Проверка деплоя

После деплоя проверьте:

- [ ] Сайт открывается на Vercel URL
- [ ] Логотип отображается
- [ ] Можно подключить кошелек
- [ ] Можно создать игру
- [ ] Игры отображаются в лобби (34 игры должны быть видны)
- [ ] База данных работает

## ⚠️ Важные замечания

### Socket.io на Vercel

Vercel **не поддерживает WebSockets** напрямую. Для Socket.io есть два варианта:

1. **Использовать отдельный сервер** для Socket.io (Railway, Render, или другой хостинг)
2. **Использовать polling** вместо WebSockets (менее оптимально, но работает)

Для production рекомендуется вынести Socket.io на отдельный сервер.

### База данных

- **Локально:** SQLite (`file:./prisma/dev.db`)
- **Production:** PostgreSQL (Vercel Postgres)

Не забудьте изменить `provider` в `prisma/schema.prisma` перед деплоем!

### Файлы, которые НЕ должны быть в git

- `.env` - переменные окружения
- `prisma/dev.db` - локальная база данных
- `prisma/seed-data.json` - экспортированные данные (опционально, можно закоммитить для бэкапа)
- `node_modules/` - зависимости
- `.next/` - билд Next.js

## 📝 После деплоя

1. Обновите README с production URL
2. Настройте кастомный домен (если нужно)
3. Настройте мониторинг и логирование
4. Протестируйте все функции на production

## 🆘 Troubleshooting

### Логотип не отображается
- Проверьте, что `public/logo.png` закоммичен в git
- Проверьте консоль браузера на ошибки 404

### База данных не работает
- Проверьте `DATABASE_URL` в переменных окружения Vercel
- Убедитесь, что миграции применены
- Проверьте логи в Vercel Dashboard

### Build fails
- Проверьте логи билда в Vercel Dashboard
- Убедитесь, что все зависимости установлены
- Проверьте, что `prisma generate` выполняется перед `next build`

