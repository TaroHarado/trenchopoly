# Деплой без базы данных

## ✅ Что исправлено:

1. **ESLint ошибки исправлены:**
   - `app/market/page.tsx` - экранирована кавычка
   - `components/GameModals.tsx` - экранирована кавычка  
   - `app/game/[gameId]/page.tsx` - добавлена зависимость в useEffect

2. **Режим работы без базы данных:**
   - `lib/prisma.ts` - автоматически определяет наличие базы данных
   - `lib/auth.ts` - использует in-memory хранилище для nonce, JWT для пользователей
   - Все API роуты возвращают пустые данные вместо ошибок

## 🚀 Деплой на Vercel БЕЗ базы данных:

### Шаг 1: Настройка переменных окружения

В Vercel → **Settings** → **Environment Variables**, добавьте **ТОЛЬКО**:

```
JWT_SECRET=<сгенерируйте случайную строку>
NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta
NEXT_PUBLIC_SOLANA_RPC_ENDPOINT=https://api.mainnet-beta.solana.com
HOUSE_WALLET_PUBLIC_KEY=EzNPUsVEBtm5tr5BeWk1V1sB2q8LnWbHZkPLfqNvFarN
SOCKET_PORT=3001
```

**НЕ добавляйте `DATABASE_URL`** - приложение будет работать без базы данных.

### Шаг 2: Build Command

В Vercel → **Settings** → **General** → **Build & Development Settings**:

**Build Command:**
```
prisma generate && next build
```

Это сгенерирует Prisma клиент (даже без базы данных) и соберет Next.js.

### Шаг 3: Деплой

Нажмите **"Deploy"** - приложение должно собраться успешно.

## ⚠️ Ограничения без базы данных:

- ❌ Игры не сохраняются (можно только просматривать)
- ❌ Нельзя создать игру
- ❌ Нельзя присоединиться к игре
- ✅ Можно подключить кошелек
- ✅ Можно открыть кейсы (данные не сохраняются)
- ✅ Можно просматривать marketplace (пустой список)
- ✅ Можно просматривать inventory (пустой список)

## 📝 Что работает:

- Лендинг страница
- Подключение кошелька
- Авторизация через JWT (без БД)
- Просмотр кейсов
- Открытие кейсов (без сохранения в БД)

## 🔄 Добавление базы данных позже:

Когда будете готовы добавить базу данных:

1. Создайте Vercel Postgres
2. Добавьте `DATABASE_URL` в переменные окружения
3. Обновите `prisma/schema.prisma` на `provider = "postgresql"`
4. Запустите миграции: `npx prisma migrate deploy`
5. Импортируйте данные: `npm run import-data`

Приложение автоматически переключится на использование базы данных.

