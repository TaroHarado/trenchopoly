# Настройка Vercel - Пошаговая инструкция

## ✅ Код уже загружен в GitHub

Репозиторий: https://github.com/TaroHarado/trenchopoly

## 📋 Шаги для деплоя на Vercel

### 1. Создание проекта в Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub (используйте тот же аккаунт, что и для репозитория)
3. Нажмите **"Add New..."** → **"Project"**
4. Найдите репозиторий `TaroHarado/trenchopoly`
5. Нажмите **"Import"**

### 2. Настройка проекта

**Framework Preset:** Next.js (должен определиться автоматически)

**Root Directory:** `./` (оставьте по умолчанию)

**Build Command:** 
```
prisma generate && prisma migrate deploy && next build
```

**Output Directory:** `.next` (по умолчанию)

**Install Command:** `npm install`

### 3. Создание Vercel Postgres базы данных

**ВАЖНО:** Сделайте это ПЕРЕД первым деплоем!

1. В панели Vercel перейдите в **Storage** (в боковом меню)
2. Нажмите **"Create Database"**
3. Выберите **"Postgres"**
4. Выберите план **"Hobby"** (бесплатный)
5. Нажмите **"Create"**
6. Дождитесь создания базы (1-2 минуты)
7. Скопируйте **Connection String** (будет показан после создания)

### 4. Настройка переменных окружения

В настройках проекта Vercel → **Settings** → **Environment Variables**, добавьте:

```
DATABASE_URL=<вставьте Connection String из шага 3>
JWT_SECRET=<сгенерируйте случайную строку, например через: openssl rand -base64 32>
NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta
NEXT_PUBLIC_SOLANA_RPC_ENDPOINT=https://api.mainnet-beta.solana.com
HOUSE_WALLET_PUBLIC_KEY=EzNPUsVEBtm5tr5BeWk1V1sB2q8LnWbHZkPLfqNvFarN
SOCKET_PORT=3001
```

**Для генерации JWT_SECRET:**
```bash
# В терминале:
openssl rand -base64 32
```

Или используйте любой генератор случайных строк (минимум 32 символа).

### 5. Обновление Prisma schema для PostgreSQL

**ВАЖНО:** Перед первым деплоем нужно изменить `prisma/schema.prisma`:

Откройте `prisma/schema.prisma` и измените:

```prisma
datasource db {
  provider = "postgresql"  // было: "sqlite"
  url      = env("DATABASE_URL")
}
```

Затем закоммитьте и запушьте изменения:

```bash
git add prisma/schema.prisma
git commit -m "Update Prisma schema for PostgreSQL production"
git push
```

### 6. Первый деплой

1. После настройки всех переменных, нажмите **"Deploy"**
2. Vercel автоматически:
   - Установит зависимости
   - Сгенерирует Prisma клиент
   - Применит миграции
   - Соберет Next.js приложение
3. Дождитесь завершения деплоя (обычно 2-5 минут)

### 7. Импорт данных

После успешного деплоя, импортируйте сохраненные данные:

**Вариант 1: Через Vercel CLI (рекомендуется)**

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Войдите в Vercel
vercel login

# Подключитесь к проекту
vercel link

# Скачайте переменные окружения
vercel env pull .env.local

# Импортируйте данные
npm run import-data
```

**Вариант 2: Через локальный .env**

```bash
# Создайте .env.local с DATABASE_URL из Vercel
echo 'DATABASE_URL="<ваш-connection-string>"' > .env.local

# Импортируйте данные
npm run import-data
```

### 8. Проверка деплоя

После импорта данных проверьте:

- [ ] Сайт открывается на Vercel URL (например: `trenchopoly.vercel.app`)
- [ ] Логотип отображается
- [ ] Можно подключить кошелек
- [ ] Можно создать игру
- [ ] Игры отображаются в лобби (34 игры должны быть видны после импорта)
- [ ] База данных работает (игры сохраняются)

## ⚠️ Важные замечания

### Socket.io на Vercel

Vercel **не поддерживает WebSockets** напрямую. 

**Варианты решения:**

1. **Использовать отдельный сервер для Socket.io:**
   - Railway (railway.app) - бесплатный план
   - Render (render.com) - бесплатный план
   - Fly.io (fly.io) - бесплатный план

2. **Использовать polling** (менее оптимально, но работает без дополнительного сервера)

Для MVP можно использовать polling, но для production рекомендуется вынести Socket.io на отдельный сервер.

### Обновление после изменений

После любых изменений в коде:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Vercel автоматически задеплоит новую версию.

## 🆘 Troubleshooting

### Build fails с ошибкой Prisma

- Убедитесь, что `DATABASE_URL` правильно настроен
- Проверьте, что в `prisma/schema.prisma` указан `provider = "postgresql"`
- Проверьте логи билда в Vercel Dashboard

### База данных не работает

- Проверьте `DATABASE_URL` в переменных окружения
- Убедитесь, что миграции применены (должны применяться автоматически при билде)
- Проверьте логи в Vercel Dashboard → Functions

### Логотип не отображается

- Убедитесь, что `public/logo.png` закоммичен в git
- Проверьте консоль браузера на ошибки 404
- Очистите кеш браузера

### Игры не отображаются

- Убедитесь, что данные импортированы (`npm run import-data`)
- Проверьте, что база данных работает
- Проверьте логи API в Vercel Dashboard

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в Vercel Dashboard
2. Проверьте переменные окружения
3. Убедитесь, что все шаги выполнены правильно

